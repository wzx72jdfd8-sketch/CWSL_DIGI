--- a/JS8.hpp
+++ b/JS8.hpp
@@ -24,32 +24,32 @@
     ORIGINAL,
     MODIFIED
     };
 
     using Array = std::array<std::array<int, 7>, 3>;
 
-    constexpr auto array = []
+    inline Array const & array(Type type)
     {
-      constexpr auto COSTAS = std::array
+      static constexpr auto COSTAS = std::array<Array, 2>
       {
-        std::array
+        Array
         {
           std::array{4, 2, 5, 6, 1, 3, 0},
           std::array{4, 2, 5, 6, 1, 3, 0},
           std::array{4, 2, 5, 6, 1, 3, 0}
         },
-        std::array
+        Array
         {
           std::array{0, 6, 2, 3, 5, 4, 1},
           std::array{1, 5, 0, 2, 3, 6, 4},
           std::array{2, 5, 0, 6, 4, 1, 3}
         }
       };
-
-      return [COSTAS](Type type) -> Array const &
-      {
-        return COSTAS[static_cast<std::underlying_type_t<Type>>(type)];
-      };
-    }();
+      return COSTAS[static_cast<std::underlying_type_t<Type>>(type)];
+    }
   }
 
   void
--- a/decodedtext.cpp
+++ b/decodedtext.cpp
@@ -192,8 +192,10 @@
     message_ = compound_ % extra_ % ' ';
 
     directed_.reserve(parts.size() - 2 + 2);
-    directed_  = { "<....>", compound_ };
-    directed_ += parts.mid(2);
+    directed_.clear();
+    directed_.append(QStringLiteral("<....>"));
+    directed_.append(compound_);
+    directed_.append(parts.mid(2));
   }
 
   return true;
